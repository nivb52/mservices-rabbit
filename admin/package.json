{
  "name": "admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "merge-schema": "npx prisma-merge -b prisma/base.prisma -o prisma/schema.prisma -s ./models/**/*.schema.prisma",
    "db": "npm run merge-schema &&  npx prisma format",
    "db:push": "npx prisma db push",
    "db:push-f": "npx prisma db push --accept-data-loss",
    "dev": "npm run nodemon src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.28.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.4.13",
    "nodemon": "^2.0.12",
    "prisma-merge": "^0.2.0",
    "typescript": "^4.3.5"
  }
}
